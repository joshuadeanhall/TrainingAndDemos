@model MBlog.Areas.Admin.Models.UserViewModel

@{
    ViewBag.Title = "Delete User";
}
<div class="alert alert-danger">You are about to delete the following user</div>

<div class="panel panel-primary">
    <div class="panel-heading">User</div>
    <div class="panel-body">
        @Html.DisplayNameFor(model => model.UserName): @Html.DisplayFor(model => model.UserName)
    </div>
</div>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    <p>
        <input class="btn btn-primary" type="submit" value="Delete" />
        |
        @Html.ActionLink("Back to List", "Index", new { }, new { @class = "btn btn-danger" })
    </p>
}
